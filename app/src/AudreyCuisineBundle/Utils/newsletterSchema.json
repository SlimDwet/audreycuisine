{
    "title": "Newsletter",
    "type": "object",
    "properties": {
        "mail": {
            "type": "string",
            "pattern": "^[0-9a-z._-]+@{1}[0-9a-z.-]{2,}[.]{1}[a-z]{2,5}$"
        }
    },
    "required": ["mail"]
}
